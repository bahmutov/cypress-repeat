/// <reference types="cypress" />

describe('cypress-repeat', () => {
  it('first', () => {
    cy.wait(100)
  })

  it('second', () => {
    cy.wait(100)
  })

  it('third', () => {
    cy.wait(100)
  })
})
